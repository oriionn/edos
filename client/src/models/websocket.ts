export enum MainMessageType {
    Login,
    Unknown,
    Availablity,
}

export enum ServerMessageType {
    Login,
    Unknown,
    SecInterval,
    MinInterval,
}

export enum WebsocketType {
    SERVER = "server",
    SERVERS = "servers",
}
